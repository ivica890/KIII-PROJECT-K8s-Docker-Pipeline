apiVersion: v1
kind: Pod
metadata:
  name: spring-boot-app
  labels:
    app: spring-boot-app
    version: "1.0"
spec:
  containers:
    - image: ivica890/spring-boot-app
      name: spring-boot-app
      ports:
        - containerPort: 8080